export interface CourseData {
    id: string | null;
    course_title: string;
    university: string;
    duration: string;
    location: string;
    fees: number;
};

export interface CourseIdData {
    id: string;
};
